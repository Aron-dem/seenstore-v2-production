{
  "version": 2,
  "buildCommand": "pnpm install && pnpm run build --filter @workspace/api-server && pnpm run build --filter @workspace/seenstore",
  "outputDirectory": "frontend/dist/public",
  "functions": {
    "api/server.mjs": { "maxDuration": 30 }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/server.mjs" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ],
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [{ "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }]
    }
  ]
}
